name: Deploy — Main Hub

on:
  push:
    branches: [main]
    paths:
      - 'src/main/**'
  workflow_dispatch:

jobs:
  deploy:
    name: Push to Google Apps Script
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Write Workload Identity credential config
        run: |
          echo '${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}' > /tmp/wif-config.json

      - name: Authenticate via Workload Identity Federation
        uses: google-github-actions/auth@v2
        with:
          credential_file: /tmp/wif-config.json
          service_account: ${{ secrets.SERVICE_ACCOUNT_EMAIL }}

      - name: Generate clasp token from service account
        run: |
          ACCESS_TOKEN=$(gcloud auth print-access-token --impersonate-service-account=${{ secrets.SERVICE_ACCOUNT_EMAIL }})
          echo "{\"token\":{\"access_token\":\"${ACCESS_TOKEN}\",\"token_type\":\"Bearer\",\"expiry_date\":9999999999999},\"oauth2ClientSettings\":{\"clientId\":\"\",\"clientSecret\":\"\",\"redirectUri\":\"\"},\"isLocalCreds\":false}" > ~/.clasprc.json

      - name: Write .clasp.json
        run: |
          echo '{"scriptId":"${{ secrets.MAIN_SCRIPT_ID }}","rootDir":"src/main"}' > .clasp.json

      - name: Push to Apps Script
        run: clasp push --force

      - name: Deploy new version
        run: |
          if [ -n "${{ secrets.MAIN_DEPLOYMENT_ID }}" ]; then
            clasp deploy --deploymentId "${{ secrets.MAIN_DEPLOYMENT_ID }}" --description "Auto-deploy $(date +'%Y-%m-%d %H:%M') — ${{ github.sha }}"
          else
            clasp deploy --description "Auto-deploy $(date +'%Y-%m-%d %H:%M') — ${{ github.sha }}"
          fi